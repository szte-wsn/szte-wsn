#include <iostream>
#include <vector>
#include <cassert>
using namespace std;

int main() {
	string m("Hello World!");
	cout <<  m << endl;
	vector<int> v;
    v.push_back(2);
    v.push_back(1);
    assert(false);
	return 0;
}
